import PayMintWebhookController from './PayMintWebhookController'
const Webhook = {
    PayMintWebhookController: Object.assign(PayMintWebhookController, PayMintWebhookController),
}

export default Webhook